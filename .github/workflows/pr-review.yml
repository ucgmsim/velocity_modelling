name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  call-review:
    uses: ucgmsim/ollama_pr_review/.github/workflows/pr-review.yml@main
    secrets:
      OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}
